void main() {
  // Crear enteros -> int
  int hora = 8;

  print(hora);

  // Crear un double -> double -> con .
  double precio = 22.25;
  print(precio);


  // Restricciones
  // int edad = 10.6; // una variable declarada como entera no puedo asignarle un valor double
  double valorPi = 3; // esto si se puede porque por detrás infiere que es un double


  // Declarar las variables de tipo num con final 
  final numeroX = 10.0;

  final numeroY = 1.1;
  print(numeroX + numeroY);
}